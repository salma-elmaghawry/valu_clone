/// The state of the user's spending limit / registration, as reported by
/// the API. Drives which (if any) attention banner shows on the home screen.
enum AccountLimitStatus { active, pendingActivation, blocked }
